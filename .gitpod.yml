tasks:
    - init: docker compose build
      command: docker compose up

ports:
    - port: 3000
      visibility: public
      onOpen: open-preview

vscode:
    extensions:
        - esbenp.prettier-vscode
        - redhat.vscode-yaml

github:
    prebuilds:
        master: true
